Utilizar el weight_per_unit nuevo del csv en los productos y utilizar ese weight en las views y cuando se anyadan productos
En new_order_item_panel

def valid_parameters?(product, quantity, weight, selected_subproducts)
    if product.nil?
      alert "Selecciona un producte."
      return false
    elsif quantity.zero?
      alert "Quantitat incorrecte"
      return false
    elsif weight.zero? and product.subproducts.empty?

cambiar producto para que tenga una propiedad (tipo_precio) que puede ser por_unidad o por_kilo.
Leer esa propiedad del csv de productos
usar esta propiedad en new_order_item_panel para saber si peso es necesario.


Las comandas tienen que poder ser exportadas y guardadas y poder ser editadas despues de haber sido generadas.

Teoricamente no deberia usar el clear en mis vistas si estan utilizan el observable pattern ?? hacer mis vistas listeners suscritos a eventos lanzados por mis clases de lib.

Add button to delete products from products view
Extend resume panel
Add panel for pig (percentages of products)

save as/load comandes (orders) in csv para poder cargarlas si se desea. Cuidado que aqui el cerdo no puede con tantas comandas?

Cada producto resta una parte del cerdo
    - Tabla producto / partes del cerdo
